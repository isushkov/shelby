footer:
  align
  bigger font
projects:
  gallery differrent sizes for projects
  add hover like horsepower
карьера - сделать страницу
index:
  animastions:
    logo во весь экран на мобильные девайсы
    add scroll animastions
    touchmove:
      $('#yourElementId').on('touchmove', function(event) {
        $(this).addClass('hover');
      });
      $('#yourElementId').on('touchend', function(event) {
        $(this).removeClass('hover');
      });

after pasandy:
  project-N:
    styles
    images
  and more pics
    add blueprints
  upd about-us:
    wait content.txt
  hosting
    +404 page
    download 3th side files, minify js, ...
  upd coreer:
    get link to indeed
